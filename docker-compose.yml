version: "2"
services:
        adjustment-reason-migration:
                image: openlmis/adjustment-reason-migration
                env_file: .env
                volumes:
                    - '/tmp:/log'
